// import spotifyApi from '../../utils/spotify';
// import { log, logLevel } from '../../utils/log';

// async function verifySpotifyConnection() {
//   try {
//     const response = await spotifyApi.getMe();
//     if (response.body) {
//       log('Connected to Spotify successfully.', logLevel.Info);
//     } else {
//       log('Failed to connect to Spotify.', logLevel.Error);
//     }
//   } catch (error) {
//     log(`Error occurred while connecting to Spotify: ${error}`, logLevel.Error);
//   }
// }

// verifySpotifyConnection();
